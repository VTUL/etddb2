<h1>Editing action</h1>

<%= form_for(@action) do |f| %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @action %> |
<%= link_to 'Back', actions_path %>
