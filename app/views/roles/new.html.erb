<h1>New role</h1>

<% if @ability.can? :assign_roles, @person %>
  <%= form_for(@role) do |f| %>
  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit 'Create' %>
  </p>
  <% end %>
<% end %>

<%= link_to 'Back', roles_path %>

