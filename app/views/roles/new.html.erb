<h1>New role</h1>

<%= form_for(@role) do |f| %>
  <% if @ability.can? :create, :role %>
  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit 'Create' %>
  </p>
  <% end %>
<% end %>

<%= link_to 'Back', roles_path %>
