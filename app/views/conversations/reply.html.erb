<%= form_for(@message, url: send_reply_to_conversation_path) do |f| %>
  <% if @message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@message.errors.count, "error") %> prohibited this message from being saved:</h2>

      <ul>
      <% @message.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :msg, "Message" %><br />
    <%= f.text_area :msg %>
  </div>
  <div class="hidden_field">
    <%= f.hidden_field(:sender_id, {value: current_person.id}) %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
