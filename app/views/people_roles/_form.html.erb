<%= form_for(@people_role) do |f| %>
  <% if @people_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@people_role.errors.count, "error") %> prohibited this person's role from being saved:</h2>

      <ul>
      <% @people_role.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :person_id %><br />
    <%= f.text_field :person_id %>
  </div>
  <div class="field">
    <%= f.label :role_id %><br />
    <%= f.collection_select(:role_id, Role.all, :id, :name) %>
  </div>
  <div class="field">
    <%= f.label :etd_id %><br />
    <%= f.text_field :etd_id %>
  </div>
  <div class="field">
    <%= f.label :vote %><br />
    <%= f.select(:vote, options_for_select([['None', ''], ['Approve', 'true'], ['Disapprove', 'false']])) %>
  </div>

  <div class="actions">
    <%= f.submit 'Submit' %>
  </div>
<% end %>
