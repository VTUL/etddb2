<%= form_for(@release_manager) do |f| %>
  <% if @release_manager.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@release_manager.errors.count, "error") %> prohibited this release_manager from being saved:</h2>

      <ul>
      <% @release_manager.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :availability_id %><br />
    <%= f.collection_select(:availability_id, Availability.where(retired: false), :id, :name) %>
  </div>
  <div class="field">
    <%= f.label :reason_id %><br />
    <%= f.colletion_select(:reason_id, Reason.all, :id, :name) %>
  </div>
  <div class="field">
    <%= f.label :other_reason_desc %><br />
    <%= f.text_field :other_reason_desc %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
