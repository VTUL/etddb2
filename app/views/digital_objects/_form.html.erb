<%= form_for(@digital_object) do |f| %>
  <% if @digital_object.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@digital_object.errors.count, "error") %> prohibited this digital object from being saved:</h2>

      <ul>
      <% @digital_object.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit 'Submit' %>
  </div>
<% end %>
