<h1>Add new role</h1>

<% form_for(@role) do |f| %>
  <!------------------------>
  <% if @ability.can? :assign_roles, @person %>
  <!-- role checkboxes go here -->
    <%=h puts "Here is Admin Mode\n"%>
    <p>
      <%= f.label :name %><br />
      <%= f.text_field :name %>
    </p>
    <p>
      <%= f.submit 'Create' %>
    </p>
  <% end %>
  <!------------------------>
<% end %>

<%= link_to 'Back', url_for(:controller=>:admin,:action=>:index_role) %>
